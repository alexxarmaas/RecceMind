# Include /api or omit it; the client normalizes both forms.
EXPO_PUBLIC_API_URL=http://127.0.0.1:8000/api

# Client-side Maps keys are visible by design. Restrict this key by app/domain and API.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
